import 'package:flutter/cupertino.dart';

class Messagens extends StatelessWidget {
  const Messagens({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("menssages"),
    );
  }
}
